Coloque aqui o logo da contabilidade com o nome `logo.png` (ou `logo.svg`,
`logo.jpg`, `logo.jpeg`) e suba esta pasta pro GitHub — o app já está
configurado para mostrar automaticamente esse arquivo no topo do menu
lateral, em todas as páginas (veja `core/branding.py`). Se o arquivo não
existir, o app não quebra — só não mostra logo nenhum.
